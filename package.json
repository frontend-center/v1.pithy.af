{
  "scripts": {
    "start": "node server.js",
    "dev": "browser-sync start --server --files=* --port=3001 --no-open --no-notify"
  },
  "dependencies": {
    "express": "^4.14.0",
    "serve-static": "^1.11.1"
  },
  "devDependencies": {
    "browser-sync": "^2.18.5"
  }
}
